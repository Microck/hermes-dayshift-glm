[project]
name = "hermes-dayshift-glm"
version = "0.1.0"
description = "Companion implementer for hermes-nightshift-glm findings and PRs"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
dayshift = "dayshift:main"

[tool.pytest.ini_options]
python_files = ["test_*.py"]
